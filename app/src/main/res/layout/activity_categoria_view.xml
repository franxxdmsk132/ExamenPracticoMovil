<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- SearchView para buscar categorías -->
    <SearchView
        android:id="@+id/searchViewCategoria"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="16dp"
        android:queryHint="Buscar categorías" />

    <!-- RecyclerView para mostrar la lista de categorías -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewCategoria"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/searchViewCategoria"
        android:layout_above="@id/buttonAgregarCategoria"
        android:layout_marginBottom="16dp"
        android:layout_marginTop="16dp" />

    <!-- Botón para agregar una nueva categoría -->
    <Button
        android:id="@+id/buttonAgregarCategoria"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:text="Agregar" />
</RelativeLayout>
